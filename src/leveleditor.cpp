#include "leveleditor.hpp"

LevelEditor::LevelEditor(SDL_Window* mainWindow) {
	_window = mainWindow;
	_editorSurface = SDL_GetWindowSurface(_window);
}

void LevelEditor::_initWorld() {
	
}

void LevelEditor::update(float delta) {
	
}